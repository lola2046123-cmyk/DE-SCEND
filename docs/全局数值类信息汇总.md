# 《暴富电梯》全局数值类信息汇总

> **版本**：与 `config/game-events.json._meta.version = 0.05` 对齐。
> **用途**：跨模块数值底表，供调平衡、对文案、写自动化测试断言时快速查阅。
> **权威顺序**：`config/game-events.json` → `js/game-controller.js` / `index.html` 内 `GameController.cfg` 读取与硬编码兜底。
> **更新**：与仓库当前逻辑一致；若改字段名或公式，请同步改本节。

---

## 1. 生涯维度（Director's Dossier）

### 1.1 存储

| 项目 | 值 |
|------|-----|
| **LocalStorage 键** | `fbc_career_archive_v1` |
| **序列化格式** | JSON 对象，UTF-8 字符串写入 |

### 1.2 字段定义与累积公式

| 字段名（JSON） | 含义（玩家侧） | 写入 / 更新公式 |
|----------------|----------------|----------------|
| `total_credits_collected` | 累计已核销资产：多局「成功撤离」到账金额之和 | `+= floor(payout)`，在 `GameController.cashOut()` 成功路径调用 `careerMerge({ creditsCollected: payout })`。 |
| `total_floors_climbed` | 累计攀爬层数 | `+= floorsJumped`（单次上行随机跳跃的层数之和），在 `processAscendComplete()` 调用 `careerMerge({ floorsClimbed, floorReached })`。 |
| `max_floor_reached` | 历史最高深度（楼层） | `= max(原值, 当前局 this.floor)`，同上 `careerMerge({ floorReached: this.floor })`。 |
| `total_liquidations` | 清算次数 | 每发生一次记入档案的清算 `+= 1`：`finishReveal()` 中未缓和 **LIQUIDATION**；`_autoLiquidate()`（Hiss 超时）。 |
| **`total_committee_overrides`**（v0.05 新增） | FBC 代决议触发次数（封存货舱选盘倒计时归零自动开柜的累计） | `+= 1`，在 `_resolveLockerSelection(committeeOverride=true)` 内 `careerMerge({ committeeOverride: true })`。 |

**加载归并**（`careerLoad`）：解析失败或缺键时按 0 处理；`max_floor_reached` 与历史取 **max**；其余数值键缺失视为 0。旧版 0.04 存档无 `total_committee_overrides`，读时回退 0，向后兼容。

**持久化**（`careerMerge`）：`careerLoad()` → 应用 `delta` → `_careerSave` 写回同一键。

### 1.3 授权面板「动态评价」（仅展示）

与档案字段联动，**不改变局内概率**（`index.html` · `updateCareerStats`）：

| 条件 | 第三行评价文案 |
|------|----------------|
| `total_liquidations >= 3` | `// 委员会对您的稳定性表示担忧。` |
| 否则若 `total_credits_collected >= 5000` | `// 您正在重塑局内的财务结构。` |
| 否则 | 不展示第三行 |

清算句优先于高资产句。

> 0.05 暂未把 `total_committee_overrides` 接入授权面板评价行；若后续要加，建议作为"清算句"之后的次优先评语，避免与稳定性叙事抢拍。

---

## 2. 节拍维度（Timing）

单位均为 **ms**。`pace(key, def)` = `GameController.cfg('uiPacing.' + key, def)`，配置非法时回退 `def`。

### 2.1 展示与决策（开门 / 结果条 / Reveal）

| 键名 / 锚点 | 配置路径或代码位置 | 当前默认（仓库快照） | 说明 |
|-------------|---------------------|----------------------|------|
| **三位一体展示框停留** | `index.html` · `showDoorResultFrame` → `setTimeout(..., 4000)` | **4000**（硬编码） | 非坠毁层结果条自动消隐；上行/撤离可提前清。 |
| **doorRevealSettleMs** | `pace('doorRevealSettleMs', 900)` | **900** | 非坠毁开门链路中，金额/特效后至 `finishReveal` 前的 settle 等待。 |
| `narrativeFlashMs` | `uiPacing.narrativeFlashMs` | **680** | 叙事闪屏覆层时长（若走该路径）。 |
| `deltaFlashMs` | `uiPacing.deltaFlashMs` | **1200** | 场景内金额闪等（与清算分支配合使用）。 |
| `afterDeltaHoldMs` | `uiPacing.afterDeltaHoldMs` | **420** | 清算路径下与 delta 后的叠加 hold。 |
| `safeNodeOverlayDelayMs` | `uiPacing.safeNodeOverlayDelayMs` | **960** | 安全屋覆层延迟。 |
| `surpriseAfterDeltaMs` | `uiPacing.surpriseAfterDeltaMs` | **320**（配置）；`pace(..., 300)` 兜底 | 惊喜视觉相对主金额节的错峰。 |
| `odometerDelayAfterOpenMs` | `uiPacing.odometerDelayAfterOpenMs` | **160** | 开门后里程表启动延迟。 |
| `passengerResyncDelayMs` | `uiPacing.passengerResyncDelayMs` | **560** | 乘客层再同步。 |
| `liquidationPanelDelayMs` | `uiPacing.liquidationPanelDelayMs` | **780** | 清算震屏后出结果面板前等待。 |

### 2.2 封存货舱选盘（v0.05 主回路节拍）

| 键 / 锚点 | 配置或代码 | 当前默认 | 说明 |
|-----------|------------|----------|------|
| `lockerSelection.countdownPerLockerMs` | `lockerSelection.countdownPerLockerMs` | **4000** / 柜 | 单只柜门贡献的倒计时基数（最终 = `count * per`，并夹在 min/max 内） |
| `lockerSelection.countdownMinMs` | `lockerSelection.countdownMinMs` | **6000** | 倒计时下限（养猪期 2 柜 = 8s 仍按下限收敛到 8s 实算；当 count 极端小时由该值兜底） |
| `lockerSelection.countdownMaxMs` | `lockerSelection.countdownMaxMs` | **16000** | 倒计时上限 |
| `lockerSelection.panicTickThresholdMs` | `lockerSelection.panicTickThresholdMs` | **3000** | 概念阈值（实际 panic 触发是 `ratio ≤ 0.3`，与本字段同向；改一边时建议同步） |
| `lockerSelection.lockerNumberMin/Max` | 同名字段 | **1 / 99** | 柜号区间，UI 渲染为两位补零（`01..99`） |
| `lockerSelection.minBidGapPositive` | 同名字段 | **1** | 一手柜中至少有 1 只为 POSITIVE/DOUBLE 的兜底约束 |
| `uiPacing.lockerOverlayShowMs` | 同名字段 | **360** | 选盘浮层从 `processAscendComplete()` 到 `showLockerOverlay()` 的延迟节拍 |
| `uiPacing.lockerSelectFlipMs` | 同名字段 | **520** | 玩家点选后柜门翻转 / 高亮动画时长（与 220ms 短延迟并行） |
| `uiPacing.lockerToManifestMs` | 同名字段 | **320** | 从选定到 `runManifestReveal()` 起点的间隔 |
| `uiPacing.manifestItemRevealMs` | 同名字段 | **620** | 单件子产物揭露动画时长 |
| `uiPacing.manifestItemHoldMs` | 同名字段 | **540** | 单件子产物 hold（与上面共用三件叠加节拍） |
| `uiPacing.manifestSettleMs` | 同名字段 | **480** | 三件清点完成后到 `finishReveal()` 的稳态等待 |
| `uiPacing.manifestItemStaggerMs` | `pace('manifestItemStaggerMs', 320)` | **320**（兜底） | UI 实际使用：每件子产物 stagger 间隔（与 `manifestItemRevealMs` 互不替代） |
| `uiPacing.manifestRevealStartMs` | `pace('manifestRevealStartMs', 220)` | **220**（兜底） | 第一件子产物的起 delay |
| `uiPacing.gainFlashHoldMultTier{1..4}` | 同名字段 | **1.32 / 1.58 / 1.98 / 2.45** | 大额收益 hold 倍率（与档位绑定） |

### 2.3 操作响应

| 键名 / 锚点 | 配置或代码 | 当前默认 | 说明 |
|-------------|------------|----------|------|
| **选柜点击 → 引擎选盘** | `index.html` · `handleLockerClick` 内 `setTimeout(..., 220)` | **220**（硬编码） | 与翻牌动画并行；玩家意志优先，禁止为等待动画而序列化阻塞结算（继承 0.04 的 300ms 设计原则） |
| **FBC 代决议自动选盘延迟** | `index.html` · 倒计时归零分支 `setTimeout(..., 240)` | **240**（硬编码） | 红色高亮 → `game.autoSelectLocker()` 的可见过渡 |
| `pre-game--clearing` | `index.html` | **300** | 清场 CSS 类移除定时器。 |

### 2.4 悬念阻塞与其它

| 键名 | 配置路径 | 当前默认 | 说明 |
|------|----------|----------|------|
| `liquidationBusyWaitMs` | `suspense.liquidationBusyWaitMs` | **1500** | 主线程同步 busy-wait（清算揭晓张力）；**0 关闭**。 |
| `lowRunwayNegativeBusyWaitMs` | `suspense.lowRunwayNegativeBusyWaitMs` | **1500** | 低跑道负面时阻塞。 |
| `lowRunwayCreditsToQuotaMax` | `suspense.lowRunwayCreditsToQuotaMax` | **0.22** | 触发低跑道负面的资产/配额比阈值。 |
| **Hiss 入侵倒计时** | `breach.durationMs` | **5000** | 超时 `_autoLiquidate`。 |
| **连击盲信窗口** | `comboBlindConfidence.windowMs` | **1500** | 决策后短窗内再上行触发增益。 |
| **BGM 静音斜坡（v0.05）** | `audio-engine.js` · `setBgmMuted` | **~220** | `_bgmBus.gain` 线性渐变到 ~0；与 `setBgmDimmed` 解耦，不影响 master/SFX。 |

---

## 3. 选盘 / 货舱 / 代决议（权力制衡的新主回路）

### 3.1 柜数与倒计时

| Biome | `lockerCountByBiome` 默认 | 养猪期（floor ≤ 3）实际 | 倒计时 = `count * 4000`，夹在 [6000, 16000] |
|-------|---------------------------|--------------------------|-----------------------------------------------|
| `admin`（1–10 楼） | **2** | **2** | 8000 → 6000（撞下限被裁到 8000 不变）｜实际 8s |
| `maintenance`（11–25 楼） | **3** | — | 12000 |
| `supernatural`（26+ 楼） | **4** | — | 16000（撞上限） |

> Biome 由 `biome.ranges` 推导；`_resolveLockerCount` 还会将柜数夹到 [2, 4]。

### 3.2 候选生成保证

`generateLockerHand(ctrl, count, quotaShift)` 内三重兜底：

1. 主循环 `count*6` 次重抽，每次独立 `rollEventFromR(r, ...)`；
2. `minBidGapPositive=1`：若 N 次抽完仍无 POSITIVE/DOUBLE，则把其中一只候选替换为正面带的拟造 outcome（`r ∈ [NEGATIVE_MAX, POSITIVE_MAX)`）；
3. 柜号 `numberMin..numberMax` 区间随机不重复，最大重抽次数 `max(40, count*12)`；若仍不足则按顺序补齐——避免确定性 RNG 测试场景下死循环。

### 3.3 FBC 代决议（committee override）

| 字段 | 默认 | 作用 |
|------|------|------|
| `fbcOverride.creditsClampRatio` | **0.62** | 代决议下账面折扣率：净增益 × ratio；净亏损 × `(1 + (1 - ratio))`，亏损反向放大 |
| `fbcOverride.extraCorruption` | **0.45** | 选盘后追加的偏移指数（与 `corruption.onCommitteeOverride` 同源，控制器实际累加 `extraCorruption`） |
| `corruption.onCommitteeOverride` | **0.45** | 与上同步；改一处时强烈建议同步另一处 |
| `fbcOverride.banner` | `FBC · 委员会代决议 · 强制开启 LOT-{{lot}}` | 浮层关闭后的委员会闪条文案 |
| `fbcOverride.messages[]` | 5 条 | 备用回音文本，目前未在 UI 直接消费，留作叙事扩展 |

代决议触发后：

- `outcome.committeeOverride = true`、`outcome.committeeOverrideLocker = lockerNumber`；
- 折扣 / 加偏移在乘客修正之后执行（避免 `creditsMod` 与 ratio 同向叠加破坏意图）；
- 生涯 `total_committee_overrides += 1`；
- 音频侧调 `audio.playCommitteeOverride()`（`AudioEngine` 桩与实现都接住）；UI 侧调 `playFbcOverrideStamp()` SFX 与 `flashCommitteePassengerDirective(banner)`。

### 3.4 选盘 SFX 时机表（v0.05）

| 时机 | 方法 | 触发逻辑 |
|------|------|----------|
| 浮层 `.active` 立即 | `playLockerSelectionEnter` | `showLockerOverlay()` 末尾 |
| 玩家点击柜门 | `playLockerOpen` | `handleLockerClick()` 调用 `selectLocker(idx)` 之前 |
| 倒计时进入 panic | `playLockerPanic` | `startLockerCountdown` 内首次 `ratio ≤ 0.3`（一次性） |
| FBC 代决议盖章 | `playFbcOverrideStamp` | 倒计时归零分支调用 `autoSelectLocker()` 之前 |
| 货舱清点单件揭露 | `playManifestRevealItem(polarity)` | `runManifestReveal` 每行 `revealed` 时根据 `delta>0/=0/<0` 取 `gain/neutral/loss` |

所有 SFX 程序化合成、直连 `master`，**绕过 BGM 总线**，因此即使在 BGM 关闭模式下也清晰可闻。

### 3.5 货舱清点：金额拆分与文案抽取

| `outcome.kind` | `splitPattern` 默认 | 三件极性提示 |
|----------------|----------------------|----------------|
| `POSITIVE` | `[0.25, 0.35, 0.40]` | 通常全 gain；负数模拟时按符号判定 |
| `DOUBLE` | `[0.20, 0.30, 0.50]` | 全 gain，最后一件最大 |
| `NEGATIVE` | `[0.30, 0.30, 0.40]` | 通常全 loss；最后一件吸收四舍五入误差 |
| `LIQUIDATION` | `[0.0, 0.0, 1.0]` | 引擎跳过清点（`runManifestReveal` 早返回 0），仅保留模式以备后续动效 |
| 其它 | `fallbackSplit = [0.33, 0.33, 0.34]` | 三件均分 |

文案池 `manifestPool[biome][polarity]` 共 6 类组合（admin/maintenance/supernatural × positive/negative/neutral），权重无放回抽取（`taken` 表去重，全空时退到完整池）。

---

## 4. 事件概率与权重

### 4.1 偏移指数（Corruption）

| 来源 | 默认值 | 备注 |
|------|--------|------|
| `corruption.max` | **3.0** | UI 条 = `corruption / 3` |
| `liquidationShift` | **0.05** | 清算带含 `min(corruption × 0.05, 0.20)` |
| `perFloor` | **0.08** | 每跳一层；CORRUPTION_SURGE 时 ×2 |
| `onPositive` / `onNegative` / `onDouble` | **0.05 / 0.12 / 0.35** | 结果带累积 |
| `onVipAuditRelief` | **−0.11** | 审查首位 VIP |
| `onDangerUnveil` | **+0.17** | 审查 DANGER 揭穿 |
| **`onCommitteeOverride`**（v0.05） | **+0.45** | FBC 代决议追加 |

**HUD 文案分档**（仅展示用）：`<20%` 稳定；`<42%` 波动；`<65%` 偏高；否则 **临界**。

### 4.2 乘客身份权重（5 类）

| id | weight | label | 关键修正 / 选盘行为 |
|----|--------|-------|---------------------|
| VIP | 28 | 监理员 | `creditsMod=1.15`、阈值降 |
| SCAMMER | 28 | 红圈商人 | `creditsMod=0.85`、100% 伪装 |
| DANGER | 22 | 希斯潜伏者 | volatile、50% 伪装 |
| **INFORMER**（v0.05） | 14 | 内线情报员 | 阈值/资本中性；`lockerHintChance=0.85` · `lockerHintAccuracy=0.78` |
| **CLEANER**（v0.05） | 8 | 清扫员 | 阈值/资本近中性；`lockerHintChance=0.45` · `lockerHintAccuracy=0.55` |

**情报抽取**（`_computeLockerHint`）：选盘前每位非伪装乘客掷一次 `lockerHintChance`；命中者按 `lockerHintAccuracy` 取最高的一位为情报源。然后：

1. 在候选柜中查找首个 POSITIVE/DOUBLE（goodIdx）与首个 NEGATIVE/LIQUIDATION（badIdx）；
2. 50% 偏向播报"好情报"，否则"坏情报"；
3. `truthful = rng() < lockerHintAccuracy`：真情报指向真实柜，假情报指向相反极性柜（无对手柜时随机）；
4. 文案从 `lockerHintCopy.good` / `bad` 池均匀抽取，`${locker}` 替换为 `01..99`。

### 4.3 选盘候选独立掷骰

每只柜独立调 `rollEventFromR`，使用同一份 `corruption / passengers / activeEnvEvent / quotaShift / floor` 上下文。`outcome.creditsMultiplier` 在生成时已固化，`selectLocker` 时不会重新掷骰，仅施加副作用（乘客修正、volatile、代决议折扣等）。

### 4.4 Lore（开门叙事 / 异动档案）

**来源**：`js/game-controller.js` · `OUTCOME_NARRATIVE_POOL` + `getOutcomeNarrative(outcome)`。
**抽取规则**：先按本层 **主结果类型**（DOUBLE / POSITIVE 高或低倍 / GOLDEN / NEGATIVE / LIQUIDATION）选子池，再在子池内 **均匀随机** 一条（`floor(rng * pool.length)`），**无 weight 数组**。

| 子池 | 条目数 | 条件（摘要） |
|------|--------|--------------|
| DOUBLE | 3 | `outcome.kind === 'DOUBLE'` |
| POSITIVE_HIGH | 3 | POSITIVE 且倍率 ≥ ~1.16 |
| POSITIVE_LOW | 2 | POSITIVE 且未达高倍 |
| GOLDEN_FLOOR | 2 | POSITIVE 且 `goldenFloor` |
| NEGATIVE | 3 | NEGATIVE |
| LIQUIDATION | 1 | LIQUIDATION |

每条含 `tag`、`text`、`lore`；`lore` 供档案模态与三位一体「查阅异动」链路使用。

> 0.05 暂未为 FBC 代决议专门写一组 lore，而是复用上述子池，再叠加 `fbcOverride.banner` 横幅；后续可在 `OUTCOME_NARRATIVE_POOL` 增加 `COMMITTEE_OVERRIDE` 子池供模态消费。

---

## 5. 交叉索引

| 主题 | 主要配置节 / 代码 |
|------|-------------------|
| 生涯键与 merge | `game-controller.js` · `_FBC_CAREER_KEY`、`careerMerge`（含 `total_committee_overrides`） |
| UI 节拍 | `uiPacing` · `index.html` · `pace()` |
| 选盘 | `lockerSelection` · `generateLockerHand` · `STATES.LOCKER_SELECTING` |
| 货舱清点 | `manifestReveal` · `manifestPool` · `runManifestReveal` |
| FBC 代决议 | `fbcOverride` · `corruption.onCommitteeOverride` · `_resolveLockerSelection(committeeOverride=true)` |
| 选盘情报 | `passenger.types.{INFORMER,CLEANER}` · `lockerHintCopy` · `_computeLockerHint` |
| 选盘 SFX | `audio-engine.js` 的 `playLocker*` / `playFbcOverrideStamp` / `playManifestRevealItem` |
| 同步悬念 | `suspense` |
| 入侵 | `breach` |

---

## 6. v0.05 删减项（保留索引以便排错）

旧版本中存在但 0.05 已删除的数值节点，引擎不再消费；如旧 LocalStorage / 旧配置仍写有这些键，将被静默忽略：

| 删除项 | 历史来源 | 替代品 |
|--------|----------|--------|
| `cardSelection.*` | 0.03–0.04 卡牌选取 | `lockerSelection` + `manifestReveal` |
| `rerollBailout` / `rerollCurrentFloor` | 0.03 重掷买断 | 选盘本身的 N 选 1 即"决策前的二次窗口" |
| `chainLightning` / `fbc_chain_debt_v1`（LocalStorage） | 0.03 连环爆雷 | — |
| `assetFreeze` | 0.03 资产冻结 | — |

---

*本文档为数值底表，不重复产品话术；对外说明仍以 `docs/产品设计-面向用户.md` 与 `docs/游戏全局文案.md` 为准。*
